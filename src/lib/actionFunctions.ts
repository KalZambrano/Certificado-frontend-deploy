export function eliminar(nombre: string) {
    alert("Función de eliminar aún no implementada para: " + nombre);
}

export function editar(nombre: string) {
    alert("Función de editar aún no implementada para: " + nombre);
}